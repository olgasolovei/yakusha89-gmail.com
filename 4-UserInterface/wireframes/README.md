@startuml
start

:Користувач відкриває сторінку авторизації;
:Завантаження сторінки <3с (NFR1);
:Користувач вводить логін та пароль;
:Система перевіряє правильність даних (NFR2);

if (Дані вірні?) then (так)
  :Авторизація успішна;
else (ні)
  :Помилка авторизації;
  stop
endif

partition "Моніторинг тросів" {
    fork
        :Система аналізує стан тросів (FR-R1);
        :Логування подій у БД (FR-R3);
    fork again
        :Користувач переглядає Dashboard та графіки;
    end fork
}

if (Високий ризик?) then (так)
    fork
        :Надіслати сигнал/Push користувачам (FR-R2);
    fork again
        :Автоматичне зупинення підйому (FR-R4);
    end fork
endif

partition "Звіти та налаштування" {
    fork
        :Користувач створює звіт;
        :Обробка запиту ≤3с (NFR3);
        :Отримання даних з БД;
        :Відображення звіту ≤3 кліки (NFR5);
    fork again
        :Користувач змінює налаштування;
        :Автозбереження змін (NFR6);
    end fork
}

stop
@enduml

